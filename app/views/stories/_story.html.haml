%li.story.col-md-2.col-sm-3.col-xs-6{class: "length-#{story.length_category}"}
  = link_to story do
    .thumbnail
      = image_tag story.cover_image.url(:thumb)
    %h4
      = rating_dot(story)
      = truncate(story.name, length: 30)
    = badge(story)
  .creators
    = creator_list(story.people, person)
