%h1 Listing creators

%table
  %tr
    %th Name
    %th Image
    %th Bio
    %th
    %th
    %th

  - @creators.each do |creator|
    %tr
      %td= creator.name
      %td= creator.image
      %td= creator.bio
      %td= link_to 'Show', creator
      %td= link_to 'Edit', edit_creator_path(creator)
      %td= link_to 'Destroy', creator, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Creator', new_creator_path
